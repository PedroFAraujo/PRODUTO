server:
  port: 8083

spring:
  application:
    name: produto

  data:
    mongodb:
      auto-index-creation: true
      uri: mongodb://localhost:27017/cliente

services:
  cliente:
    url: http://localhost:8082